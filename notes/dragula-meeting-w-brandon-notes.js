  // ref with a function will invoke the function with the element it's a ref of

  // we have access to document within the react lifecycle methods

  // don't want to alter DOM, just query and reverse it
  // if you alter DOM, do it via props or state so that react will still know how to diff

  // can always bring in stuff like jquery and underscore if we want